{
    "development":{
        "PORT": "4000",
        "DATABASE_URL": "postgresql://postgres:password@localhost:5432/midespensa"
    },
    "test":{
        "PORT": "8080",
        "DATABASE_URL": "postgresql://postgres:password@localhost:5432/midespensatest"
    }
}
